# This is a Databricks asset bundle definition for VG_First_DAB_WithParam.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: VG_First_DAB_WithParam
  uuid: 2bccf11d-a39d-4cc3-96c5-a007642e39aa

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-86135934-e538.cloud.databricks.com
    variables:
      catalog: workspace
      schema: dev
  prod:
    mode: production
    workspace:
      host: https://dbc-86135934-e538.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/svijayaganeshsp@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/svijayaganeshsp@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: workspace
      schema: prod
    permissions:
      - user_name: svijayaganeshsp@gmail.com
        level: CAN_MANAGE
